{%- comment -%}
  Brand Promise
  Two-column promise section: visual (image or icon grid) + heading/copy.
  Fully configurable via Theme Editor settings + blocks.
{%- endcomment -%}

{%- liquid
  assign sid = section.id
  assign layout_reverse = false
  if section.settings.media_position == 'right'
    assign layout_reverse = true
  endif

  assign icon_count = 0
  for block in section.blocks
    if block.type == 'icon' and block.settings.enabled
      if block.settings.image != blank or block.settings.title != blank
        assign icon_count = icon_count | plus: 1
      endif
    endif
  endfor

  assign show_icon_grid = false
  if section.settings.media_mode == 'icons' and icon_count > 0
    assign show_icon_grid = true
  endif
  if section.settings.media_mode == 'auto' and icon_count > 0
    assign show_icon_grid = true
  endif
-%}

{%- style -%}
  #bpp-{{ sid }} {
    --bpp-bg: {{ section.settings.color_background }};
    --bpp-heading: {{ section.settings.color_heading }};
    --bpp-text: {{ section.settings.color_text }};
    --bpp-label: {{ section.settings.color_label }};
    --bpp-accent: {{ section.settings.color_accent }};
    --bpp-icon-title: {{ section.settings.color_icon_title }};
    --bpp-surface: {{ section.settings.color_surface }};
    --bpp-radius: {{ section.settings.media_radius }}px;
    --bpp-gap: {{ section.settings.column_gap }}px;
    --bpp-container: {% if section.settings.full_width %}100%{% else %}{{ section.settings.container_width }}px{% endif %};
    --bpp-pad-top-d: {{ section.settings.padding_top_desktop }}px;
    --bpp-pad-bottom-d: {{ section.settings.padding_bottom_desktop }}px;
    --bpp-pad-top-m: {{ section.settings.padding_top_mobile }}px;
    --bpp-pad-bottom-m: {{ section.settings.padding_bottom_mobile }}px;
    --bpp-heading-size-d: {{ section.settings.heading_size_desktop }}px;
    --bpp-heading-size-m: {{ section.settings.heading_size_mobile }}px;
    --bpp-text-size: {{ section.settings.text_size }}px;
    --bpp-label-size: {{ section.settings.label_size }}px;
    --bpp-icon-title-size: {{ section.settings.icon_title_size }}px;
    --bpp-icon-size: {{ section.settings.icon_size }}px;
    --bpp-media-max: {{ section.settings.media_max_width }}px;
    --bpp-copy-max: {{ section.settings.copy_max_width }}px;
    --bpp-ease: cubic-bezier(0.22, 1, 0.36, 1);
    --bpp-shadow:
      {% case section.settings.shadow_strength %}
        {% when 'none' %}none
        {% when 'soft' %}0 8px 28px rgba(49, 36, 38, 0.04)
        {% when 'medium' %}0 12px 32px rgba(49, 36, 38, 0.06)
        {% when 'strong' %}0 18px 44px rgba(49, 36, 38, 0.09)
      {% endcase %};
  }

  #bpp-{{ sid }} {
    position: relative;
    isolation: isolate;
    background-color: #ffffff;
    padding-top: var(--bpp-pad-top-m);
    padding-bottom: var(--bpp-pad-bottom-m);
    color: var(--bpp-text);
    overflow-x: clip;
    overflow-y: visible;
  }

  /* Soft brand wash — no hard section border */
  #bpp-{{ sid }}::before {
    content: '';
    position: absolute;
    inset: 0;
    z-index: 0;
    pointer-events: none;
    background:
      linear-gradient(
        180deg,
        color-mix(in srgb, #f6f1f2 35%, transparent) 0%,
        transparent 28%,
        transparent 58%,
        color-mix(in srgb, #f6f1f2 28%, transparent) 100%
      ),
      radial-gradient(
        ellipse 70% 50% at 50% 100%,
        color-mix(in srgb, #ece4e6 40%, transparent),
        transparent 70%
      );
  }

  #bpp-{{ sid }} .bpp__inner {
    position: relative;
    z-index: 1;
    width: 100%;
    max-width: var(--bpp-container);
    margin: 0 auto;
    padding-left: 1.25rem;
    padding-right: 1.25rem;
    box-sizing: border-box;
  }

  #bpp-{{ sid }} .bpp__grid {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 2rem;
  }

  #bpp-{{ sid }} .bpp__media,
  #bpp-{{ sid }} .bpp__copy {
    width: 100%;
  }

  #bpp-{{ sid }} .bpp__media {
    max-width: var(--bpp-media-max);
  }

  #bpp-{{ sid }} .bpp__copy {
    max-width: var(--bpp-copy-max);
    text-align: {{ section.settings.copy_alignment }};
    {% if section.settings.copy_alignment == 'center' %}
      margin-left: auto;
      margin-right: auto;
    {% elsif section.settings.copy_alignment == 'right' %}
      margin-left: auto;
    {% endif %}
  }

  #bpp-{{ sid }} .bpp__label {
    margin: 0 0 0.75rem;
    font-size: var(--bpp-label-size);
    font-weight: 500;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--bpp-label);
  }

  #bpp-{{ sid }} .bpp__heading {
    margin: 0 0 1rem;
    font-family: var(--font-heading-family, Georgia, 'Times New Roman', serif);
    font-style: var(--font-heading-style, normal);
    font-weight: {{ section.settings.heading_weight }};
    font-size: var(--bpp-heading-size-m);
    line-height: 1.12;
    letter-spacing: -0.025em;
    color: var(--bpp-heading);
    text-wrap: balance;
  }

  #bpp-{{ sid }} .bpp__text {
    margin: 0;
    font-size: var(--bpp-text-size);
    line-height: 1.7;
    color: var(--bpp-text);
  }

  #bpp-{{ sid }} .bpp__text p {
    margin: 0 0 0.85rem;
  }

  #bpp-{{ sid }} .bpp__text p:last-child {
    margin-bottom: 0;
  }

  #bpp-{{ sid }} .bpp__text strong {
    font-weight: 700;
    color: var(--bpp-heading);
  }

  /* Single promise mark above copy */
  #bpp-{{ sid }} .bpp__promise-mark {
    --bpp-promise-size: {{ section.settings.promise_icon_size | default: 64 }}px;
    display: flex;
    justify-content: {% if section.settings.copy_alignment == 'left' %}flex-start{% elsif section.settings.copy_alignment == 'right' %}flex-end{% else %}center{% endif %};
    margin: 0 0 1.15rem;
  }

  #bpp-{{ sid }} .bpp__promise-mark-inner {
    width: var(--bpp-promise-size);
    height: var(--bpp-promise-size);
    display: flex;
    align-items: center;
    justify-content: center;
    color: {{ section.settings.color_promise_icon | default: '#6F4B53' }};
  }

  #bpp-{{ sid }} .bpp__promise-mark-inner svg,
  #bpp-{{ sid }} .bpp__promise-mark-inner img {
    width: 100%;
    height: 100%;
    display: block;
    object-fit: contain;
  }

  /* —— Single image: layered showcase —— */
  #bpp-{{ sid }} .bpp__image-stage {
    position: relative;
    width: 100%;
    perspective: 1200px;
    padding: 0.85rem 0.75rem 1.25rem;
    box-sizing: border-box;
  }

  #bpp-{{ sid }} .bpp__image-stack {
    position: relative;
    transform-style: preserve-3d;
    {% if section.settings.enable_media_depth %}
      {% if section.settings.media_position == 'right' %}
        transform: rotateY(-5deg) rotateX(2deg);
      {% else %}
        transform: rotateY(5deg) rotateX(2deg);
      {% endif %}
      transition: transform 0.85s var(--bpp-ease);
    {% endif %}
  }

  {% if section.settings.enable_media_depth %}
    #bpp-{{ sid }} .bpp__image-plane {
      position: absolute;
      inset: 0;
      border-radius: calc(var(--bpp-radius) + 4px);
      pointer-events: none;
    }

    #bpp-{{ sid }} .bpp__image-plane--back {
      background: color-mix(in srgb, var(--bpp-accent) 28%, var(--bpp-surface));
      transform: translate3d(
        {% if section.settings.media_position == 'right' %}-14px{% else %}14px{% endif %},
        14px,
        -28px
      );
      box-shadow: 0 18px 40px rgba(28, 22, 16, 0.08);
    }

    #bpp-{{ sid }} .bpp__image-plane--mid {
      background: color-mix(in srgb, var(--bpp-accent) 12%, #fff);
      opacity: 0.65;
      transform: translate3d(
        {% if section.settings.media_position == 'right' %}-7px{% else %}7px{% endif %},
        7px,
        -14px
      );
    }

    @media (hover: hover) {
      #bpp-{{ sid }} .bpp__image-stage:hover .bpp__image-stack {
        transform: rotateY(0deg) rotateX(0deg) translateY(-4px);
      }
    }
  {% else %}
    #bpp-{{ sid }} .bpp__image-plane {
      display: none;
    }
  {% endif %}

  #bpp-{{ sid }} .bpp__image-wrap {
    position: relative;
    z-index: 1;
    border-radius: var(--bpp-radius);
    overflow: hidden;
    box-shadow:
      0 1px 0 rgba(255, 255, 255, 0.55) inset,
      var(--bpp-shadow);
    background: color-mix(in srgb, var(--bpp-accent) 8%, var(--bpp-surface));
    {% case section.settings.image_aspect %}
      {% when 'square' %}
        aspect-ratio: 1 / 1;
      {% when 'portrait' %}
        aspect-ratio: 4 / 5;
      {% when 'landscape' %}
        aspect-ratio: 4 / 3;
      {% else %}
        aspect-ratio: auto;
    {% endcase %}
  }

  #bpp-{{ sid }} .bpp__image-wrap::after {
    content: '';
    position: absolute;
    inset: 0;
    pointer-events: none;
    background:
      linear-gradient(145deg, rgba(255, 255, 255, 0.22) 0%, transparent 36%, transparent 68%, rgba(28, 22, 16, 0.08) 100%);
    z-index: 1;
  }

  #bpp-{{ sid }} .bpp__image-wrap img {
    width: 100%;
    height: 100%;
    object-fit: {{ section.settings.image_fit }};
    object-position: center;
    display: block;
    {% if section.settings.hover_zoom %}
      transition: transform 1s var(--bpp-ease);
    {% endif %}
  }

  {% if section.settings.hover_zoom %}
    @media (hover: hover) {
      #bpp-{{ sid }} .bpp__image-stage:hover .bpp__image-wrap img {
        transform: scale(1.04);
      }
    }
  {% endif %}

  #bpp-{{ sid }} .bpp__image-placeholder {
    width: 100%;
    min-height: 240px;
    aspect-ratio: 4 / 5;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--bpp-accent);
    opacity: 0.4;
    border-radius: var(--bpp-radius);
    background: color-mix(in srgb, var(--bpp-accent) 10%, var(--bpp-surface));
  }

  /* —— Icon grid: light fill + unique polygonal frames —— */
  #bpp-{{ sid }} .bpp__icons {
    list-style: none;
    margin: 0;
    padding: 0.25rem 0;
    display: grid;
    grid-template-columns: repeat({{ section.settings.icons_per_row_mobile }}, minmax(0, 1fr));
    gap: {{ section.settings.icon_gap }}px;
  }

  #bpp-{{ sid }} .bpp__icon {
    text-align: center;
    margin: 0;
  }

  #bpp-{{ sid }} .bpp__icon-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    background: transparent;
    border: 0;
    box-shadow: none;
    padding: 0.35rem 0.25rem;
    border-radius: 0;
    overflow: visible;
  }

  #bpp-{{ sid }} .bpp__icon-card::before {
    display: none;
  }

  #bpp-{{ sid }} .bpp__icon-glow {
    display: none;
  }

  #bpp-{{ sid }} .bpp__icon-media {
    position: relative;
    z-index: 1;
    width: var(--bpp-icon-size);
    height: var(--bpp-icon-size);
    max-width: 100%;
    margin: 0 auto 0.85rem;
    box-sizing: border-box;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
    background: {{ section.settings.color_icon_bg | default: '#FBF8F4' }};
    box-shadow:
      0 4px 14px rgba(28, 22, 16, 0.07),
      0 1px 3px rgba(28, 22, 16, 0.04);
    overflow: hidden;
    transition: transform 0.65s var(--bpp-ease), box-shadow 0.65s var(--bpp-ease);
  }

  #bpp-{{ sid }} .bpp__icon-media-inner {
    width: 100%;
    height: 100%;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  {% if section.settings.enable_media_depth %}
    @media (hover: hover) {
      #bpp-{{ sid }} .bpp__icon:hover .bpp__icon-media {
        transform: translateY(-4px);
        box-shadow:
          0 10px 24px rgba(28, 22, 16, 0.1),
          0 2px 6px rgba(28, 22, 16, 0.05);
      }
    }
  {% endif %}

  #bpp-{{ sid }} .bpp__icon-media img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  #bpp-{{ sid }} .bpp__icon-title {
    position: relative;
    z-index: 1;
    margin: 0;
    font-size: var(--bpp-icon-title-size);
    font-weight: 500;
    line-height: 1.4;
    letter-spacing: 0.02em;
    color: var(--bpp-icon-title);
  }

  /* Animations */
  #bpp-{{ sid }} .bpp-anim {
    opacity: 0;
    transform: translateY(16px);
    transition:
      opacity 0.8s var(--bpp-ease),
      transform 0.8s var(--bpp-ease);
  }

  #bpp-{{ sid }} .bpp-anim.is-visible {
    opacity: 1;
    transform: none;
  }

  #bpp-{{ sid }} .bpp__icon.bpp-anim {
    transition-delay: calc(var(--bpp-stagger, 0) * 80ms);
  }

  @media (prefers-reduced-motion: reduce) {
    #bpp-{{ sid }} .bpp-anim {
      opacity: 1;
      transform: none;
      transition: none;
    }

    #bpp-{{ sid }} .bpp__image-wrap img,
    #bpp-{{ sid }} .bpp__image-stack,
    #bpp-{{ sid }} .bpp__icon-media {
      transition: none !important;
      transform: none !important;
      animation: none !important;
    }
  }

  @media screen and (min-width: 750px) {
    #bpp-{{ sid }} {
      padding-top: var(--bpp-pad-top-d);
      padding-bottom: var(--bpp-pad-bottom-d);
    }

    #bpp-{{ sid }} .bpp__inner {
      padding-left: 2rem;
      padding-right: 2rem;
    }

    #bpp-{{ sid }} .bpp__grid {
      flex-direction: row;
      align-items: center;
      gap: var(--bpp-gap);
      {% if layout_reverse %}
        flex-direction: row-reverse;
      {% endif %}
    }

    #bpp-{{ sid }} .bpp__media,
    #bpp-{{ sid }} .bpp__copy {
      flex: 1 1 0;
    }

    #bpp-{{ sid }} .bpp__heading {
      font-size: var(--bpp-heading-size-d);
    }

    #bpp-{{ sid }} .bpp__icons {
      grid-template-columns: repeat({{ section.settings.icons_per_row_desktop }}, minmax(0, 1fr));
    }
  }

  @media screen and (min-width: 990px) {
    #bpp-{{ sid }} .bpp__inner {
      padding-left: 2.5rem;
      padding-right: 2.5rem;
    }
  }
{%- endstyle -%}

<section
  id="bpp-{{ sid }}"
  class="bpp"
  {% if section.settings.heading != blank %}
    aria-labelledby="bpp-heading-{{ sid }}"
  {% else %}
    aria-label="{{ section.settings.label | default: 'Our promise' | escape }}"
  {% endif %}
  data-bpp-section
>
  <div class="bpp__inner">
    <div class="bpp__grid">
      <div class="bpp__media bpp-anim" data-bpp-anim>
        {%- if show_icon_grid -%}
          <ul class="bpp__icons" role="list">
            {%- assign icon_index = 0 -%}
            {%- for block in section.blocks -%}
              {%- if block.type == 'icon' and block.settings.enabled -%}
                {%- if block.settings.image != blank or block.settings.title != blank -%}
                  <li
                    class="bpp__icon bpp-anim"
                    data-bpp-anim
                    style="--bpp-stagger: {{ icon_index }};"
                    {{ block.shopify_attributes }}
                  >
                    <div class="bpp__icon-card">
                      <div class="bpp__icon-media">
                        <div class="bpp__icon-media-inner">
                          {%- if block.settings.image != blank -%}
                            {%- assign icon_alt = block.settings.title | default: section.settings.heading -%}
                            {{
                              block.settings.image
                              | image_url: width: 400
                              | image_tag:
                                loading: 'lazy',
                                decoding: 'async',
                                widths: '120, 200, 300, 400',
                                sizes: '(min-width: 750px) 140px, 30vw',
                                alt: icon_alt
                            }}
                          {%- else -%}
                            <svg xmlns="http://www.w3.org/2000/svg" width="36" height="36" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.4" aria-hidden="true">
                              <path d="M12 3 14.5 8.5 20 9.3 16 13.4 17 19 12 16.3 7 19 8 13.4 4 9.3 9.5 8.5z"/>
                            </svg>
                          {%- endif -%}
                        </div>
                      </div>
                      {%- if block.settings.title != blank -%}
                        <p class="bpp__icon-title">{{ block.settings.title | escape }}</p>
                      {%- endif -%}
                    </div>
                  </li>
                  {%- assign icon_index = icon_index | plus: 1 -%}
                {%- endif -%}
              {%- endif -%}
            {%- endfor -%}
          </ul>
        {%- elsif section.settings.image != blank -%}
          <div class="bpp__image-stage">
            <div class="bpp__image-stack">
              {%- if section.settings.enable_media_depth -%}
                <div class="bpp__image-plane bpp__image-plane--back" aria-hidden="true"></div>
                <div class="bpp__image-plane bpp__image-plane--mid" aria-hidden="true"></div>
              {%- endif -%}
              <div class="bpp__image-wrap">
                {%- assign main_alt = section.settings.image_alt | default: section.settings.heading -%}
                {{
                  section.settings.image
                  | image_url: width: 1400
                  | image_tag:
                    loading: 'lazy',
                    decoding: 'async',
                    widths: '400, 600, 800, 1000, 1200, 1400',
                    sizes: '(min-width: 990px) 520px, (min-width: 750px) 45vw, 90vw',
                    alt: main_alt
                }}
              </div>
            </div>
          </div>
        {%- else -%}
          <div class="bpp__image-placeholder" role="img" aria-label="Promise image placeholder">
            <svg xmlns="http://www.w3.org/2000/svg" width="64" height="64" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.3" aria-hidden="true">
              <rect x="3" y="5" width="18" height="14" rx="2"/>
              <circle cx="8.5" cy="10" r="1.5"/>
              <path d="m21 16-5-4-4 3-3-2-4 3"/>
            </svg>
          </div>
        {%- endif -%}
      </div>

      <div class="bpp__copy">
        {%- if section.settings.show_promise_icon -%}
          <div class="bpp__promise-mark bpp-anim" data-bpp-anim aria-hidden="true">
            <span class="bpp__promise-mark-inner">
              {%- if section.settings.promise_icon_image != blank -%}
                {{
                  section.settings.promise_icon_image
                  | image_url: width: 400
                  | image_tag:
                    loading: 'lazy',
                    decoding: 'async',
                    widths: '64, 96, 128, 200, 300, 400',
                    sizes: '(min-width: 750px) 96px, 72px',
                    alt: ''
                }}
              {%- else -%}
                {%- case section.settings.promise_icon -%}
                  {%- when 'shield' -%}
                    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 3 20 7v5c0 5-3.4 8.4-8 9.5C7.4 20.4 4 17 4 12V7z"/><path d="m9 12 2 2 4-4"/></svg>
                  {%- when 'check' -%}
                    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9"/><path d="m8.5 12.5 2.5 2.5 4.5-5"/></svg>
                  {%- when 'hand' -%}
                    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"><path d="M8 13V7a1.5 1.5 0 0 1 3 0v4"/><path d="M11 11V6a1.5 1.5 0 0 1 3 0v5"/><path d="M14 11V7.5a1.5 1.5 0 0 1 3 0V14a5 5 0 0 1-5 5h-1.5a6.5 6.5 0 0 1-5.8-3.5L6 13"/><path d="M6 13a1.5 1.5 0 0 1 2 0"/></svg>
                  {%- else -%}
                    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2l2.2 2.2L17 3.5l.7 2.8L20.5 7l-1.3 2.5.8 2.8-2.7.8L16 16l-2.2-.7L12 17.5l-1.8-2.2L8 16l-.7-2.9-2.7-.8.8-2.8L4.1 7l2.8-.7L7.6 3.5 10 4.2z"/><path d="m9 12 2 2 4-4"/></svg>
                {%- endcase -%}
              {%- endif -%}
            </span>
          </div>
        {%- endif -%}

        {%- if section.settings.label != blank -%}
          <p class="bpp__label bpp-anim" data-bpp-anim>{{ section.settings.label | escape }}</p>
        {%- endif -%}

        {%- if section.settings.heading != blank -%}
          <h2 id="bpp-heading-{{ sid }}" class="bpp__heading bpp-anim" data-bpp-anim>
            {{ section.settings.heading | escape }}
          </h2>
        {%- endif -%}

        {%- if section.settings.text != blank -%}
          <div class="bpp__text rte bpp-anim" data-bpp-anim>
            {{ section.settings.text }}
          </div>
        {%- endif -%}
      </div>
    </div>
  </div>
</section>

<script>
  (function () {
    var root = document.getElementById('bpp-{{ sid }}');
    if (!root) return;

    var items = root.querySelectorAll('[data-bpp-anim]');
    var reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

    if (reduceMotion || !('IntersectionObserver' in window)) {
      items.forEach(function (el) { el.classList.add('is-visible'); });
      return;
    }

    var observer = new IntersectionObserver(
      function (entries) {
        entries.forEach(function (entry) {
          if (!entry.isIntersecting) return;
          entry.target.classList.add('is-visible');
          observer.unobserve(entry.target);
        });
      },
      { threshold: 0.18, rootMargin: '0px 0px -30px 0px' }
    );

    items.forEach(function (el) { observer.observe(el); });
  })();
</script>

{% schema %}
{
  "name": "Brand Promise",
  "tag": "section",
  "class": "section-brand-promise",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "text",
      "id": "label",
      "label": "Small label",
      "default": "Clean Compatible"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Our Promise"
    },
    {
      "type": "richtext",
      "id": "text",
      "label": "Description",
      "default": "<p>Not just <strong>free of harmful and toxic chemicals</strong>, but made with ingredients that enhance the beauty of every piece and support lasting quality and craftsmanship.</p>"
    },
    {
      "type": "select",
      "id": "copy_alignment",
      "label": "Text alignment",
      "options": [
        { "value": "left", "label": "Left" },
        { "value": "center", "label": "Center" },
        { "value": "right", "label": "Right" }
      ],
      "default": "center"
    },
    {
      "type": "header",
      "content": "Promise icon"
    },
    {
      "type": "checkbox",
      "id": "show_promise_icon",
      "label": "Show promise icon above text",
      "default": true
    },
    {
      "type": "image_picker",
      "id": "promise_icon_image",
      "label": "Custom icon image",
      "info": "Upload your own icon. If set, this replaces the built-in icon."
    },
    {
      "type": "select",
      "id": "promise_icon",
      "label": "Built-in icon",
      "options": [
        { "value": "seal", "label": "Seal (promise)" },
        { "value": "shield", "label": "Shield (trust)" },
        { "value": "check", "label": "Check (verified)" },
        { "value": "hand", "label": "Hand (from us)" }
      ],
      "default": "seal",
      "info": "Used when no custom icon is uploaded."
    },
    {
      "type": "range",
      "id": "promise_icon_size",
      "min": 40,
      "max": 120,
      "step": 2,
      "unit": "px",
      "label": "Icon size",
      "default": 64
    },
    {
      "type": "header",
      "content": "Media"
    },
    {
      "type": "select",
      "id": "media_mode",
      "label": "Media type",
      "options": [
        { "value": "auto", "label": "Auto (icons if added, else image)" },
        { "value": "image", "label": "Single image" },
        { "value": "icons", "label": "Icon grid" }
      ],
      "default": "auto"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Main image"
    },
    {
      "type": "text",
      "id": "image_alt",
      "label": "Image alt text",
      "default": "Our brand promise"
    },
    {
      "type": "select",
      "id": "image_aspect",
      "label": "Image aspect ratio",
      "options": [
        { "value": "natural", "label": "Natural" },
        { "value": "square", "label": "Square" },
        { "value": "portrait", "label": "Portrait (4:5)" },
        { "value": "landscape", "label": "Landscape (4:3)" }
      ],
      "default": "portrait"
    },
    {
      "type": "select",
      "id": "image_fit",
      "label": "Image fit",
      "options": [
        { "value": "cover", "label": "Cover" },
        { "value": "contain", "label": "Contain" }
      ],
      "default": "cover"
    },
    {
      "type": "range",
      "id": "media_radius",
      "min": 0,
      "max": 40,
      "step": 2,
      "unit": "px",
      "label": "Media border radius",
      "default": 16
    },
    {
      "type": "checkbox",
      "id": "hover_zoom",
      "label": "Enable image hover zoom",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "enable_media_depth",
      "label": "Enable 3D depth effects",
      "default": true,
      "info": "Layered depth on the main image + soft lift on icons."
    },
    {
      "type": "select",
      "id": "shadow_strength",
      "label": "Image shadow",
      "options": [
        { "value": "none", "label": "None" },
        { "value": "soft", "label": "Soft" },
        { "value": "medium", "label": "Medium" },
        { "value": "strong", "label": "Strong" }
      ],
      "default": "medium"
    },
    {
      "type": "header",
      "content": "Icon grid"
    },
    {
      "type": "range",
      "id": "icon_size",
      "min": 48,
      "max": 160,
      "step": 4,
      "unit": "px",
      "label": "Icon size",
      "default": 88
    },
    {
      "type": "range",
      "id": "icon_gap",
      "min": 12,
      "max": 48,
      "step": 2,
      "unit": "px",
      "label": "Icon gap",
      "default": 28
    },
    {
      "type": "select",
      "id": "icons_per_row_desktop",
      "label": "Icons per row (desktop)",
      "options": [
        { "value": "2", "label": "2" },
        { "value": "3", "label": "3" },
        { "value": "4", "label": "4" }
      ],
      "default": "2"
    },
    {
      "type": "select",
      "id": "icons_per_row_mobile",
      "label": "Icons per row (mobile)",
      "options": [
        { "value": "1", "label": "1" },
        { "value": "2", "label": "2" }
      ],
      "default": "2"
    },
    {
      "type": "select",
      "id": "icon_shape",
      "label": "Icon shape",
      "options": [
        { "value": "rounded", "label": "Rounded" },
        { "value": "circle", "label": "Circle" }
      ],
      "default": "rounded"
    },
    {
      "type": "range",
      "id": "icon_radius",
      "min": 0,
      "max": 32,
      "step": 2,
      "unit": "px",
      "label": "Icon corner radius",
      "default": 12,
      "info": "Used when shape is Rounded."
    },
    {
      "type": "header",
      "content": "Layout"
    },
    {
      "type": "select",
      "id": "media_position",
      "label": "Media position (desktop)",
      "options": [
        { "value": "left", "label": "Media left" },
        { "value": "right", "label": "Media right" }
      ],
      "default": "left"
    },
    {
      "type": "checkbox",
      "id": "full_width",
      "label": "Full width",
      "default": false
    },
    {
      "type": "range",
      "id": "container_width",
      "min": 900,
      "max": 1600,
      "step": 20,
      "unit": "px",
      "label": "Container width",
      "default": 1200
    },
    {
      "type": "range",
      "id": "column_gap",
      "min": 24,
      "max": 96,
      "step": 4,
      "unit": "px",
      "label": "Column gap",
      "default": 48
    },
    {
      "type": "range",
      "id": "media_max_width",
      "min": 280,
      "max": 720,
      "step": 10,
      "unit": "px",
      "label": "Media max width",
      "default": 560
    },
    {
      "type": "range",
      "id": "copy_max_width",
      "min": 280,
      "max": 640,
      "step": 10,
      "unit": "px",
      "label": "Copy max width",
      "default": 480
    },
    {
      "type": "range",
      "id": "padding_top_desktop",
      "min": 0,
      "max": 140,
      "step": 4,
      "unit": "px",
      "label": "Desktop padding top",
      "default": 80
    },
    {
      "type": "range",
      "id": "padding_bottom_desktop",
      "min": 0,
      "max": 140,
      "step": 4,
      "unit": "px",
      "label": "Desktop padding bottom",
      "default": 80
    },
    {
      "type": "range",
      "id": "padding_top_mobile",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Mobile padding top",
      "default": 48
    },
    {
      "type": "range",
      "id": "padding_bottom_mobile",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Mobile padding bottom",
      "default": 48
    },
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "color",
      "id": "color_background",
      "label": "Background",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "color_surface",
      "label": "Surface tint",
      "default": "#EFE7DD"
    },
    {
      "type": "color",
      "id": "color_heading",
      "label": "Heading",
      "default": "#1F1A14"
    },
    {
      "type": "color",
      "id": "color_text",
      "label": "Body text",
      "default": "#4A433C"
    },
    {
      "type": "color",
      "id": "color_label",
      "label": "Label",
      "default": "#9A7B4F"
    },
    {
      "type": "color",
      "id": "color_accent",
      "label": "Accent",
      "default": "#A27780"
    },
    {
      "type": "color",
      "id": "color_icon_title",
      "label": "Icon title",
      "default": "#2A241C"
    },
    {
      "type": "color",
      "id": "color_icon_bg",
      "label": "Icon background",
      "default": "#FBF8F4"
    },
    {
      "type": "color",
      "id": "color_promise_icon",
      "label": "Promise icon color",
      "default": "#8A6A4A"
    },
    {
      "type": "header",
      "content": "Typography"
    },
    {
      "type": "range",
      "id": "heading_size_desktop",
      "min": 20,
      "max": 56,
      "step": 1,
      "unit": "px",
      "label": "Heading size (desktop)",
      "default": 32
    },
    {
      "type": "range",
      "id": "heading_size_mobile",
      "min": 18,
      "max": 40,
      "step": 1,
      "unit": "px",
      "label": "Heading size (mobile)",
      "default": 26
    },
    {
      "type": "select",
      "id": "heading_weight",
      "label": "Heading weight",
      "options": [
        { "value": "400", "label": "Regular" },
        { "value": "500", "label": "Medium" },
        { "value": "600", "label": "Semi-bold" },
        { "value": "700", "label": "Bold" }
      ],
      "default": "600"
    },
    {
      "type": "range",
      "id": "text_size",
      "min": 13,
      "max": 20,
      "step": 1,
      "unit": "px",
      "label": "Body text size",
      "default": 15
    },
    {
      "type": "range",
      "id": "label_size",
      "min": 10,
      "max": 16,
      "step": 1,
      "unit": "px",
      "label": "Label size",
      "default": 12
    },
    {
      "type": "range",
      "id": "icon_title_size",
      "min": 12,
      "max": 18,
      "step": 1,
      "unit": "px",
      "label": "Icon title size",
      "default": 14
    }
  ],
  "blocks": [
    {
      "type": "icon",
      "name": "Icon",
      "settings": [
        {
          "type": "checkbox",
          "id": "enabled",
          "label": "Enable",
          "default": true
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Icon / image"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Natural ingredients"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Brand Promise",
      "blocks": [
        {
          "type": "icon",
          "settings": {
            "enabled": true,
            "title": "Clean Compatible"
          }
        },
        {
          "type": "icon",
          "settings": {
            "enabled": true,
            "title": "Thoughtfully formulated"
          }
        },
        {
          "type": "icon",
          "settings": {
            "enabled": true,
            "title": "Quality craftsmanship"
          }
        },
        {
          "type": "icon",
          "settings": {
            "enabled": true,
            "title": "Made with care"
          }
        }
      ]
    }
  ]
}
{% endschema %}
